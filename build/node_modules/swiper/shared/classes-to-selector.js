'use strict';

function classesToSelector(classes = '') {
  return `.${classes.trim().replace(/([\.:!\/])/g, '\\$1') // eslint-disable-line
  .replace(/ /g, '.')}`;
}

module.exports = classesToSelector;
//# sourceMappingURL=classes-to-selector.js.map
