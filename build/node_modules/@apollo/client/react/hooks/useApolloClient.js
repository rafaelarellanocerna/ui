'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

require('../../utilities/globals/index.js');
var React = require('react');
var ApolloContext = require('../context/ApolloContext.js');
var invariant = require('../../../../ts-invariant/lib/invariant.js');

function useApolloClient(override) {
    var context = React.useContext(ApolloContext.getApolloContext());
    var client = override || context.client;
    __DEV__ ? invariant.invariant(!!client, 'Could not find "client" in the context or passed in as an option. ' +
        'Wrap the root component in an <ApolloProvider>, or pass an ApolloClient ' +
        'instance in via options.') : invariant.invariant(!!client, 29);
    return client;
}

exports.useApolloClient = useApolloClient;
//# sourceMappingURL=useApolloClient.js.map
