'use strict';

function getMainAxisFromPlacement(placement) {
  return ['top', 'bottom'].indexOf(placement) >= 0 ? 'x' : 'y';
}

module.exports = getMainAxisFromPlacement;
//# sourceMappingURL=getMainAxisFromPlacement.js.map
